name: Build Module
on:
 workflow_dispatch:

jobs:
  build:
    name: Creating Module Zip
    runs-on: windows-latest
    
    steps:
      - uses: actions/checkout@v3
      - run: 7z
      - run: cmd help